# Prisma/PostgreSQL Skill

Inspect prisma/schema.prisma before database changes. Prefer additive migrations. Preserve idempotency for billing, webhook, ledger, and payout flows. Never run destructive production migrations automatically. Validate schema generation and migrations before merge.
