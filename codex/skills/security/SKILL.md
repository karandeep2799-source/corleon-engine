# Security Skill

Treat secrets, production databases, payment systems, identity systems, and production deployments as protected resources. Use least privilege. Validate all tool arguments. Log privileged actions without logging secrets. Require approval for destructive database operations and production deployment. Never bypass authentication or authorization controls.
