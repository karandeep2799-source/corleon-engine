# Vercel Skill

Use the deployment lifecycle: inspect -> build -> test -> preview -> verify -> promote. Preview deployments are the default target for agent changes. Production deployment requires explicit human approval. Never print or retrieve deployment secrets.
